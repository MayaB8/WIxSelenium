package com.Wix_Selenium.elements;

import org.openqa.selenium.WebDriver;

public class Link extends GeneralWebElement {
    public Link(WebDriver browser, String xpath, String tag) {
        super(browser, xpath, tag);
    }
}