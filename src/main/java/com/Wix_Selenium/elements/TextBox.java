package com.Wix_Selenium.elements;

import org.openqa.selenium.WebDriver;

public class TextBox extends GeneralWebElement {
    public TextBox(WebDriver browser, String xpath, String tag) {
        super(browser, xpath, tag);
    }
}