package com.Wix_Selenium.elements;

import org.openqa.selenium.WebDriver;

public class Button extends com.Wix_Selenium.elements.GeneralWebElement {
    public Button(WebDriver browser, String xpath, String tag) {
        super(browser, xpath, tag);
    }
}